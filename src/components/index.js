import { Buttons, Inputs, Selects, Text } from "./ui";

export const { BtnPostulation, BtnClose, BtnSubmit } = Buttons;
export const { InputSearch, InputForm } = Inputs;
export const { SelectForm } = Selects;
export const { Title } = Text;
